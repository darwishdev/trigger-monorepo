<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Activities tied to opportunities -->

        <record id="activity_call_opp1" model="mail.activity">
            <field name="res_model_id" ref="crm.model_crm_lead"/>
            <field name="res_id" ref="opp_1"/>
            <field name="activity_type_id" ref="mail.mail_activity_data_call"/>
            <field name="summary">Call Nour to confirm private showing slot</field>
            <field name="date_deadline" eval="(DateTime.now() + relativedelta(days=2)).strftime('%Y-%m-%d')"/>
            <field name="user_id" ref="base.user_admin"/>
            <field name="state">planned</field>
        </record>

        <record id="activity_meeting_opp3" model="mail.activity">
            <field name="res_model_id" ref="crm.model_crm_lead"/>
            <field name="res_id" ref="opp_3"/>
            <field name="activity_type_id" ref="mail.mail_activity_data_meeting"/>
            <field name="summary">Sign exclusivity agreement with Hossam Abdel-Aziz</field>
            <field name="date_deadline" eval="(DateTime.now() + relativedelta(days=5)).strftime('%Y-%m-%d')"/>
            <field name="user_id" ref="base.user_admin"/>
            <field name="state">planned</field>
        </record>

        <record id="activity_email_opp4" model="mail.activity">
            <field name="res_model_id" ref="crm.model_crm_lead"/>
            <field name="res_id" ref="opp_4"/>
            <field name="activity_type_id" ref="mail.mail_activity_data_email"/>
            <field name="summary">Send marketing proposal revisions to Nile Capital</field>
            <field name="date_deadline" eval="(DateTime.now() + relativedelta(days=1)).strftime('%Y-%m-%d')"/>
            <field name="user_id" ref="base.user_admin"/>
            <field name="state">planned</field>
        </record>

        <record id="activity_call_opp5" model="mail.activity">
            <field name="res_model_id" ref="crm.model_crm_lead"/>
            <field name="res_id" ref="opp_5"/>
            <field name="activity_type_id" ref="mail.mail_activity_data_call"/>
            <field name="summary">Confirm final counter-offer with Khaled &amp; closing attorney</field>
            <field name="date_deadline" eval="(DateTime.now() + relativedelta(days=3)).strftime('%Y-%m-%d')"/>
            <field name="user_id" ref="base.user_admin"/>
            <field name="state">planned</field>
        </record>

        <record id="activity_meeting_opp6" model="mail.activity">
            <field name="res_model_id" ref="crm.model_crm_lead"/>
            <field name="res_id" ref="opp_6"/>
            <field name="activity_type_id" ref="mail.mail_activity_data_meeting"/>
            <field name="summary">Lease signing with Prestige PM and tenant</field>
            <field name="date_deadline" eval="(DateTime.now() + relativedelta(days=7)).strftime('%Y-%m-%d')"/>
            <field name="user_id" ref="base.user_admin"/>
            <field name="state">planned</field>
        </record>

        <record id="activity_todo_opp2" model="mail.activity">
            <field name="res_model_id" ref="crm.model_crm_lead"/>
            <field name="res_id" ref="opp_2"/>
            <field name="activity_type_id" ref="mail.mail_activity_data_todo"/>
            <field name="summary">Prepare pro-forma analysis for Sheikh Zayed duplex</field>
            <field name="date_deadline" eval="(DateTime.now() + relativedelta(days=4)).strftime('%Y-%m-%d')"/>
            <field name="user_id" ref="base.user_admin"/>
            <field name="state">planned</field>
        </record>

        <!-- Activity on a fresh lead -->
        <record id="activity_call_lead1" model="mail.activity">
            <field name="res_model_id" ref="crm.model_crm_lead"/>
            <field name="res_id" ref="lead_inquiry_1"/>
            <field name="activity_type_id" ref="mail.mail_activity_data_call"/>
            <field name="summary">Qualify Omar - confirm budget &amp; financing</field>
            <field name="date_deadline" eval="(DateTime.now() + relativedelta(days=1)).strftime('%Y-%m-%d')"/>
            <field name="user_id" ref="base.user_admin"/>
            <field name="state">planned</field>
        </record>

        <record id="activity_email_lead3" model="mail.activity">
            <field name="res_model_id" ref="crm.model_crm_lead"/>
            <field name="res_id" ref="lead_inquiry_3"/>
            <field name="activity_type_id" ref="mail.mail_activity_data_email"/>
            <field name="summary">Send available Class A office listings to El-Saeed Holding</field>
            <field name="date_deadline" eval="(DateTime.now() + relativedelta(days=2)).strftime('%Y-%m-%d')"/>
            <field name="user_id" ref="base.user_admin"/>
            <field name="state">planned</field>
        </record>

    </data>
</odoo>
